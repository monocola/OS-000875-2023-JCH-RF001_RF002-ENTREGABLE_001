apiVersion: v1
kind: Service
metadata:
  labels:
    app: entidad-mdp-dev
  name: entidad-mdp-dev
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: entidad-mdp-dev